<div style="display:flex; justify-content:space-between; align-items:center; border-bottom:1px solid #000; padding:10px;">
  <div style="font-weight:bold;">Timeline</div>

  <% if (user) { %>
    <div>Welcome <%= user.name %></div>
    <a href="/logout">Log off</a>
  <% } else { %>
    <a href="/login">Login</a>
  <% } %>
</div>